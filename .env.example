# Copie para .env (gitignored). Defaults locais de dev — NÃO use em prod.
POSTGRES_USER=radar
POSTGRES_PASSWORD=radar
POSTGRES_DB=radar
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
DATABASE_URL=postgresql://radar:radar@localhost:5432/radar
